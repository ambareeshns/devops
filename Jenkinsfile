pipeline {
    agent { label 'ambi_slave' }
    stages {
        stage("maven instalation"){
            steps{
                sh "sudo apt install maven"
            }
        }
        
        
    }
    
}

pipeline {
    agent { label 'slave1' }
    stages {
        stage("maven instalation"){
            steps{
                sh "apt install maven"
            }
        }
        
        
    }
    
}
